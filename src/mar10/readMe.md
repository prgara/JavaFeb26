Constructor ? 
Employee ->  Employee (){}

this ? refers to current obj


// wap to get the sum of all the ele
int[][] arr = {{2,5,6},{8,2,7}}; //30

row = arr.length
col = arr[i].length

static is a reserved keyword, loaded in memory before even any object is created.
- variable -> shared by all the objects, belongs to the class, memory optimization
- method -> Can be accessed with class reference instead of object reference means we dont need to create an object
- block -> called automatically and have the highest preference. even called before the instance block
- class

type of block
- instance block - gets called when we create an object
- static block

java have 5 memory areas
- heap -> objects stored here
- stack
- method area -> static stored here
- PC register
- Native method stack

Garbage collection


Student class - name, school name
