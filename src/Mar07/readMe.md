
# Constructor
- It is a special method which is used to initialize the object.
- No return type
- same name as class name
types of constructor
- Default Constructor -> by default its there no constr in class
- Parameterized Constructor -> const with params but once defined in class then default const will not be there.
- you have to explicitly define it.


this keyword -> it belongs to the current object



create a class student with instnace var name, age, rollNo, majorSubject and have both the constructors to initialize the var and then create 2 objects of this class to print the o/p


Arrays -> 2d arrays

create a 2d array and take user input to fill data in array and then use for loop to print it




















